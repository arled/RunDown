import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  inputField: {
    height: 40,
    borderColor: 'grey',
    borderBottomWidth: 1,
    margin: 20,
  },
})

export default styles
