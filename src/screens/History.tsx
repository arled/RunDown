import * as React from 'react'
import { Text } from 'react-native'
import ScrollLayout from '@rundown-components/ScrollLayout'

const History = () => (
  <ScrollLayout testID="history screen">
    <Text>History</Text>
  </ScrollLayout>
)

export default History
